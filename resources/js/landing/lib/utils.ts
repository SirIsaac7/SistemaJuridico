import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

// `getInitials` vivía aquí y ya no: devolvía una letra por palabra, así que
// «Claudia Daniela Flores Aguilar» salía como «CDFA» mientras el resto del
// panel mostraba «CA». Las iniciales del avatar las calcula ahora un único
// sitio, `lib/avatar.ts` (`inicialesAvatar`), que usa el componente
// `UserAvatar`. Ver también `colorAvatar`, que fija el color de cada persona.

export function formatCurrency(
  amount: number,
  opts?: {
    currency?: string;
    locale?: string;
    minimumFractionDigits?: number;
    maximumFractionDigits?: number;
    noDecimals?: boolean;
  },
) {
  const { currency = "USD", locale = "en-US", minimumFractionDigits, maximumFractionDigits, noDecimals } = opts ?? {};

  const formatOptions: Intl.NumberFormatOptions = {
    style: "currency",
    currency,
    minimumFractionDigits: noDecimals ? 0 : minimumFractionDigits,
    maximumFractionDigits: noDecimals ? 0 : maximumFractionDigits,
  };

  return new Intl.NumberFormat(locale, formatOptions).format(amount);
}
